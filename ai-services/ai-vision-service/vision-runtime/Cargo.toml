[package]
name = "vision-runtime"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "Pipeline de visão — domain, tracking, preprocess. IPC VPE1."
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
serde_json = "1"
